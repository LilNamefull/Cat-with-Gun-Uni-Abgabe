using UnityEngine;

public class PowerUp : MonoBehaviour
{

    public bool activateShield;  // Boolean flag to activate the shield
    public bool addGuns;        // Boolean flag to add guns or upgrade weaponry
    public bool invincible;     // Boolean flag to make the player invincible


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
